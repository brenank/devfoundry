# Managed by devfoundry via chezmoi.
# Shared Git defaults come from one of two places:
# - /etc/gitconfig on NixOS system-default machines
# - ~/.config/git/config generated by devfoundry and pointing at ~/.local/share/devfoundry in user-owned mode
#
# This file is only for user and machine overrides.

[include]
    path = ~/.config/devfoundry/git/host.gitconfig

[include]
    path = ~/.config/devfoundry/git/local.gitconfig
